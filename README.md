# ComPoM

ComPoM is an R package for compositional modeling and post-processing of model outputs, with tools for data preparation, model fitting, and visualization.

## Installation

You can install the development version from your local source using:

```r
# In the package root director
# Replace 'yourusername/ComPoM' with the actual repo
devtools::install_github("/ComPoM")